import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useAuth } from '../components/Auth';
export default function Login(){const{login}=useAuth(),nav=useNavigate(),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{await login(String(f.get('username')),String(f.get('password')));nav('/admin');}catch(err:any){setError(err.response?.data?.message||'Unable to sign in');}finally{setBusy(false);}};return <main className="login-page"><section className="login-brand"><span className="brand-tile">SP</span><h1>SmartPortfolio</h1><p>Your content, identity, and digital presence — in one professional workspace.</p></section><section className="login-panel"><form onSubmit={submit}><div className="login-heading"><span>ADMIN WORKSPACE</span><h2>Welcome back</h2><p>Sign in to manage your portfolio.</p></div>{error&&<div className="alert alert-danger">{error}</div>}<label>Username<input name="username" autoComplete="username" required autoFocus/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label><button className="admin-primary" disabled={busy}>{busy?'Signing in…':'Sign in securely'}</button><small>The first password is configured securely during setup.</small></form></section></main>}

